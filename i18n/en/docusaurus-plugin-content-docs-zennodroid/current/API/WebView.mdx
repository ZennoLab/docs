---
sidebar_position: 9
sidebar_label: WebView API
title: WebView API | ZennoDroid Documentation | ZennoLab
description: WebView API - section of the official ZennoDroid documentation by ZennoLab. Detailed guides, setup instructions, and usage examples
---
import DisclaimerNotice from '@site/src/components/DisclaimerNotice';

# WebView API
<DisclaimerNotice />

## Description  
**`IWebViewAPI WebView`**  

Gives you control over the WebView, which is mainly used in browsers (and rarely in other apps).  

The main advantage of WebView is that you can interact with web page content using JavaScript, which seriously expands what you can do with websites.  
_______________________________________________
## Methods  
### Run JavaScript
```csharp
string EvaluateScript(string script)
```   

*Parameters:*   
```csharp
string script // JavaScript code.
``` 

#### Example  
```csharp
var webView = instance.DroidInstance.WebView;

var script = "return document.documentElement.innerHTML"; // JS to get the page html
var html = webView.EvaluateScript(script); // Run the script and get the html
```
_______________________________________________
### Get WebSocket URL
```csharp
string GetAttachedWebSocketUrl()
```   

#### Example  
```csharp
var webView = instance.DroidInstance.WebView;

var webSocket = webView.GetAttachedWebSocketUrl(); // Get the WebSocket
```
_______________________________________________
### System certificate injection in Android.
```csharp
void InjectSystemCertificate(string source)
```   

#### Example.  
```csharp
var webView = instance.DroidInstance.WebView;

webView.InjectSystemCertificate(project.Variables["cert_pem"].Value);
```  
_______________________________________________
