---
sidebar_position: 1
sidebar_label: Info API
title: Info API | Документация ZennoDroid | ZennoLab
description: Info API - раздел официальной документации ZennoDroid от ZennoLab. Подробные инструкции, примеры использования и руководство по настройке
---
import DisclaimerNotice from '@site/src/components/DisclaimerNotice';

# Info API
<DisclaimerNotice />

## Описание. 
**`IDroidInfoAPI Info`**  

Предоставляет доступ к информации о текущем устройстве. Этот интерфейс используется для получения различных данных о подключенном устройстве после его выбора и подключения через методы интерфейса IDroidActionAPI Action.  
_______________________________________________
### Свойства.  
- **Индекс устройства**.
```csharp
int Index { get; }
```  
Дублирует информацию **Индекс** из окна [Менеджер устройств](../Tools/Device_Manager). 
_______________________________________________
- **Название устройства**.
```csharp
string Title { get; }
```  
Дублирует информацию **Название** из окна [Менеджер устройств](../Tools/Device_Manager).
_______________________________________________
- **Имя устройства (ADB)**.
```csharp
string Name { get; }
```  
Имя ADB устройства, дублирует информацию **Устройство** из окна [Менеджер устройств](../Tools/Device_Manager).
_______________________________________________
- **Порт отображаемый в окне инстанса**.
```csharp
int UiPort { get; }
```  
Актуально только при выполнении шаблона в ZennoDroid.
_______________________________________________
- **Адрес подключения через ADB**.
```csharp
string AddressPort { get; }
```  
Для физических телефонов совпадает со значением свойства *Name*. Для эмуляторов содержит адрес и порт подключения, например "127.0.0.1:21503" для MEmu.
_______________________________________________  
#### Пример.  
```csharp
var info = instance.DroidInstance.Info;

int index = info.Index;
string title = info.Title;
string Name = info.Name;
int uiPort = info.UiPort;
string addressPort = info.AddressPort;
```  