---
sidebar_position: 8
sidebar_label: Proxy API
title: Proxy API | Документация ZennoDroid | ZennoLab
description: Proxy API - раздел официальной документации ZennoDroid от ZennoLab. Подробные инструкции, примеры использования и руководство по настройке
---
import DisclaimerNotice from '@site/src/components/DisclaimerNotice';

# Proxy API
<DisclaimerNotice />

## Описание.  
**`IDroidProxyAPI Proxy`**  

Этот интерфейс нужен для управления настройками прокси-сервера на устройстве. Он позволяет настроить базовые параметры прокси для устройства, а также дополнительные настройки, необходимые для тонкой конфигурации подключения.  

_______________________________________________
## Методы.
### Установить прокси.
```csharp
void SetProxy(string proxy, IDroidProxySettings settings)
```   

*Параметры:*   
```csharp
string proxy // Значение прокси. Формат: login:pass@IP:port
``` 

```csharp
IDroidProxySettings settings // Параметры прокси. Например: new DroidProxySettings.ProxifierViaPC()
``` 

#### Пример.  
```csharp
var proxy = instance.DroidInstance.Proxy;

// Установить прокси
var settings = new DroidProxySettings.ClashViaPC();
settings.DnsServers = "1.1.1.1";
instance.DroidInstance.Proxy.SetProxy("socks5://login:pass@100.20.30.40:8080", settings);
```
_______________________________________________ 
### Получить прокси проекта.
```csharp
string GetProxy()
```    

#### Пример.  
```csharp
var proxy = instance.DroidInstance.Proxy;

var projectProxy = proxy.GetProxy(); // Получить прокси проекта
```
_______________________________________________ 
### Очистить прокси.
```csharp
void ClearProxy()
```    

#### Пример.  
```csharp
var proxy = instance.DroidInstance.Proxy;

proxy.ClearProxy(); // Очистить прокси проекта
```
_______________________________________________ 
### Включить частный DNS.
```csharp
void EnablePrivateDns(string address)
```    

*Параметры:*   
```csharp
string address // Частный DNS.
``` 

#### Пример.  
```csharp
var proxy = instance.DroidInstance.Proxy;

proxy.EnablePrivateDns("dns.google"); // Включить частный DNS
```
_______________________________________________ 
### Отключить частный DNS.
```csharp
void DisablePrivateDns()
```    

*Параметры:*   
```csharp
string address // Частный DNS.
``` 

#### Пример.  
```csharp
var proxy = instance.DroidInstance.Proxy;

proxy.DisablePrivateDns(); // Отключить частный DNS
```
_______________________________________________ 
